from data import question_data


class Question:
    def __init__(self ,question, answer):
        self.question = question
        self.answer = answer

for text in range(len(question_data) - 1)
    question = Question(text[text],text[])


question_1 = Question("The united state is a developed nation","True")
